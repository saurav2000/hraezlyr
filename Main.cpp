#include "Tree.h"
#include <iostream>
#include <chrono>
using namespace std;

int State::N = 1;

int main()
{
	int id,n,m,time_limit;
	cin>>id;
	cin>>n;
	cin>>m;
	cin>>time_limit;
	id = 1-id*2;
	// int State::N = n;
	// int State::M = m;
	// int State::LIMIT = n*m;
	// int Cannon::N = n;
	// int Cannon::M = m;
	// int Cannon::LIMIT = n*m;
	while(true)
	{
		break;
	}
	return 0;
}